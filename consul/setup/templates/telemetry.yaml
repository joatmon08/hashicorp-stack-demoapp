global:
  metrics:
    enableTelemetryCollector: true
  cloud:
    resourceId:
      secretKey: resource-id
      secretName: consul-hcp-observability
    clientId:
      secretKey: client-id
      secretName: consul-hcp-observability
    clientSecret:
      secretKey: client-secret
      secretName: consul-hcp-observability

telemetryCollector:
  enabled: true
  cloud:
    clientId:
      secretKey: client-id
      secretName: consul-hcp-observability
    clientSecret:
      secretKey: client-secret
      secretName: consul-hcp-observability